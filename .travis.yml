language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-tnsw-external-map-app' ]; then cd .. && eval "mv $currentfolder generator-tnsw-external-map-app" && cd generator-tnsw-external-map-app; fi
